# SmartStocks

The React (Vite) frontend lives in **`frontend/`**. See [frontend/README.md](frontend/README.md) for the file manifest, routes, and UI notes.

```bash
cd frontend
npm install
npm run dev
```
